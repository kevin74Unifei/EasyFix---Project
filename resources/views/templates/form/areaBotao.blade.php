        <div class="buttons">            
            <button action="submit" class="btn btn-primary" >
                @if(isset($resp))
                    Alterar
                @else
                    Cadastrar
                @endif
            </button>  
        </div>
</form>   

